#include<stdio.h>
/*
bool veri tipi true yada false deðerlerini alýr

mantýksal ifadeler doðru ise true
mantýksal ifadeler yanlýþ ise false
ayný zamanda bütün 0 olmayan sayýlar 1,230,23332,vs true deðer sayýlýr.
0 ise false sayýlýr


Mantýksal Operatörler
< küçük müdür. a<b þeklinde
> büyük müdür. a>b þeklinde
<= küçük ya da eþit midir a<=b þeklinde.
>= büyük yada eþit midir a>=b þeklinde.
== eþit midir a==b þeklinde.
!= eþit deðil midir.a!=b þeklinde

Mantýksal Baðlaçlar
Bir çok mantýksal ifadeler iþlemi baðlayan araçlardýr.

&& mantýksal and ("ve") baðlacý. true && true&& false gib baglanabilir.
&& olduðu cümlenin true olmasý için bütün ifadelerin true olmasý gerekir.
Eðrer bu cümlenin içinde bir tane bile false varsa o mantýksal cümle false(yanlýþ) olur.

|| mantýksal or ("ya da") baðlacý.true&& true&& false gibi baðlanabilir.
|| olduðu cümlenin true olmasý için en az bir ifadenin true olmasý gerekir.
Eðer mantýksal or (||) ile baðlanmýþ cümlenin içinde bütün ifadeler false ise o mantýksal cümle false(yanlýþ) olur.

! mantýksal not ("deðil") operatörü. doðru olan(true)olan mantksal ifadeyi yanlýþ(false) yapar.
yanlýþ olan (false) olan mantýksal ifadeyi doðru(true) yapar.

*/
int main(){
	
	
	
	
	
	return 0;
	
}
