#include <stdio.h>
int main(){
	/*
	değişken degerlerini arttırken i+=1 de yapıla bilir veya i++ da yapıla bilir
	lakin postfix ve prefix dediğimiz bir yapı var burda i++ olabilir ++i de olabilir peki farkı nedir?
	farkı şu
	diyelim ki 
	int i=4; diye bir degişkenimiz var.
	printf("%d\n",i++);
	ekrana 4 degerini basacaktır.
	çünkü bu postfix halindedir i'nin artmadan önceki degerini yazacaktır ve arttırma işlemini yazdıktan sonra yapacaktır.
	
	
	printf("%d\n",++i);
	ekrana 5 degerini basacaktır.
	çünkü bu prefix bir yapıdır ve basmadan önce i'nin değerini arttırıp sonra basacaktır bu yüzden de i degerimiz 5 olacaktır.
	
	
	i = i % a;
	
	bu formata sokmaya çalışırsak
	
	örnek: i = i * (a+1); eşiti ---> i*=a+1;
	
	i = f = k = 0;
	bu şekildeki atamalarda aynı sağdan başlayarak atama yapılır ve şu stil yazımla benzerdir
	i = 0;
	f = 0;
	k = 0;
	
	*/
	
	
	
	
	
	return 0;
}
